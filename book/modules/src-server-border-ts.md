---
type: "module"
id: "module:src/server/border.ts"
title: "src/server/border.ts"
standing: "built"
standing_source: "knowledge/artifacts.json"
source_path: "src/server/border.ts"
source_line: 1
origin: "derived"
generator: "tools/vault/emit.mjs"
aliases:
  - "module:src/server/border.ts"
---

# src/server/border.ts

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree.  
> *Declared in `knowledge/artifacts.json`.*

101 lines · 5 exported symbols.

## What the file says of itself

> The border book. When the Regent one day appoints an outside producer
> (email → arrivals, or any other), it never touches the chronicle doc —
> that is a whole-document write, last writer wins, and a foreign hand
> would clobber the book. Instead the producer drops rows into the vault's
> border_arrivals table, and the keyholder alone reconciles them: on every
> read it folds unabsorbed rows into the doc's arrivals, and once a written
> doc carries a row it marks that row absorbed so it retires from the
> merge. Everything is keyed by id, so the fold is idempotent — a failed
> absorb only means a harmless re

## Shape

- **Lines:** 101
- **Exported symbols (5):** `BorderRow`, `absorbBorderRows`, `carriedArrivalIds`, `fetchBorderRows`, `mergeBorderArrivals`

## Backlinks

### Entities

- [[The border book]] — *declared in `knowledge/entities.json`*

### Modules

- [[src/worker.ts]] — *imported by this file*

---

*Generated by `tools/vault/emit.mjs` from `src/server/border.ts`:1. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run book`).*
