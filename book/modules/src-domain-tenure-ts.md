---
type: "module"
id: "module:src/domain/tenure.ts"
title: "src/domain/tenure.ts"
standing: "built"
standing_source: "knowledge/artifacts.json"
source_path: "src/domain/tenure.ts"
source_line: 1
origin: "derived"
generator: "tools/vault/emit.mjs"
aliases:
  - "module:src/domain/tenure.ts"
---

# src/domain/tenure.ts

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree.  
> *Declared in `knowledge/artifacts.json`.*

618 lines · 36 exported symbols.

## What the file says of itself

> The tenure hierarchy — where a door sits and under whose law
> (docs/WRIT-THE-WAR-TABLE.md §5, and the ANSWERED section at its foot).
> 
> The book already had one axis: WHO HOLDS WHAT WORK — the offices, the fiefs,
> the knights, the pods, the patrons. This is the other axis, and it answers a
> different question: WHERE A DOOR STANDS AND WHOSE LAW REACHES IT. The two are
> orthogonal, which is why they compose without a fight; they meet at exactly
> two points and at both they already agree — the KNIGHT here is the knight of
> `pods.ts` (a person with a book of business), and the DOOR here is the door
> every 

## Shape

- **Lines:** 618
- **Exported symbols (36):** `CraftStanding`, `Door`, `EDICT_PRESSING_DAYS`, `EMPTY_TENURE`, `Edict`, `EdictKind`, `EdictReading`, `EdictStanding`, `FEE_HAS_NO_GEOMETRY`, `FOUNDING_TENURE`, `Fee`, `GEOMETRY_WORDS`, `GUILD_READING_IS_A_CRAFT`, `Realm`, `SHIRE_MAX_HEADLESS_CRAFTS`, `SHIRE_MIN_DOORS`, `SHIRE_MIN_KNIGHTS`, `SHIRE_STORES_NO_STANDING`, `Shire`, `ShireStanding`, `ShireStandingReading`, `TenureBook`, `doorsOfFee`, `doorsOfRealm`, `doorsOfShire`, `edictsOfRealm`, `feeOf`, `knightsOfShire`, `readEdict`, `readEdicts`, `readShireStanding`, `readShireStandings`, `realmOf`, `shireOf`, `shiresOfFee`, `unattendedDoors`

## Facts it depends on

- [[Edict pressing window]] — *the same file `src/domain/tenure.ts`, seen as a fact rather than a module*
- [[Shire promotion — headless crafts allowed]] — *the same file `src/domain/tenure.ts`, seen as a fact rather than a module*
- [[Shire promotion — minimum doors]] — *the same file `src/domain/tenure.ts`, seen as a fact rather than a module*
- [[Shire promotion — minimum knights]] — *the same file `src/domain/tenure.ts`, seen as a fact rather than a module*

## Entities

- [[Fee (an owner group, tenure.ts)]] — *the same file `src/domain/tenure.ts`, seen as a entity rather than a module*
- [[Realm (a place — tenure.ts)]] — *the same file `src/domain/tenure.ts`, seen as a entity rather than a module*
- [[Shire / March (a metro's standing)]] — *the same file `src/domain/tenure.ts`, seen as a entity rather than a module*

## Modules

- [[src/domain/guilds.ts]] — *imported by this file*

## Backlinks

### Facts it depends on

- [[Edict pressing window]] — *declared in `knowledge/facts.json`; the same file `src/domain/tenure.ts`, seen as a module rather than a fact*
- [[Shire promotion — headless crafts allowed]] — *declared in `knowledge/facts.json`; the same file `src/domain/tenure.ts`, seen as a module rather than a fact*
- [[Shire promotion — minimum doors]] — *declared in `knowledge/facts.json`; the same file `src/domain/tenure.ts`, seen as a module rather than a fact*
- [[Shire promotion — minimum knights]] — *declared in `knowledge/facts.json`; the same file `src/domain/tenure.ts`, seen as a module rather than a fact*

### Entities

- [[Fee (an owner group, tenure.ts)]] — *the same file `src/domain/tenure.ts`, seen as a module rather than a entity*
- [[Realm (a place — tenure.ts)]] — *the same file `src/domain/tenure.ts`, seen as a module rather than a entity*
- [[Shire / March (a metro's standing)]] — *the same file `src/domain/tenure.ts`, seen as a module rather than a entity*

### Modules

- [[src/domain/tenureMuster.ts]] — *imported by this file*

## Documents that cite this source

*These name this FILE by its path. That is a citation of the source, not a claim about any one idea inside it — do not read a path citation as agreement, dependence or implementation.*

- [[Writ — the War Table (the front-end direction)]]

---

*Generated by `tools/vault/emit.mjs` from `src/domain/tenure.ts`:1. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run book`).*
