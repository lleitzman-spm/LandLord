---
type: "module"
id: "module:src/wargame-core.ts"
title: "src/wargame-core.ts"
standing: "built"
standing_source: "knowledge/artifacts.json"
source_path: "src/wargame-core.ts"
source_line: 1
origin: "derived"
generator: "tools/vault/emit.mjs"
aliases:
  - "module:src/wargame-core.ts"
---

# src/wargame-core.ts

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree.  
> *Declared in `knowledge/artifacts.json`.*

20 lines · 10 exported symbols.

## What the file says of itself

> The war-game dealer, bundled for raw Node — the tooling twin of
> `operator-core.ts`.
> 
> The domain modules import each other extensionlessly (`from './catalog'`),
> which a real bundler resolves and raw Node does not. That is why the harness
> gets `operator-core.mjs` instead of importing `src/domain` directly. Dealing
> a War Game needs a different, larger slice of the domain than the operator's
> readings do, and the operator bundle is imported by the deployed keyholder —
> so this is a SEPARATE entry rather than more surface on that one.
> 
> Everything here is already public domain API; this file only gath

## Shape

- **Lines:** 20
- **Exported symbols (10):** `SIGNALS`, `WAR_HOUSEHOLD`, `dealtGame`, `economyOf`, `generateGrandMuster`, `generateWarGame`, `isHouseholdUpkeep`, `normalizeChronicle`, `routeAgentEvents`, `takenAgentEventIds`

## Modules

- [[src/domain/agentIntake.ts]] — *imported by this file*
- [[src/domain/chronicle.ts]] — *imported by this file*
- [[src/domain/treasury.ts]] — *imported by this file*
- [[src/domain/wargame.ts]] — *imported by this file*

## Backlinks

### Writs that specify it

- [[The Kingdom — Canon]] — *this writ names the exported symbol `generateGrandMuster`; this writ names the exported symbol `generateWarGame`*
- [[The PM Task-and-Process Library (reference)]] — *this writ names the exported symbol `normalizeChronicle`*
- [[Writ — the flow engine (the operator's spine)]] — *this writ names the exported symbol `normalizeChronicle`*
- [[Writ — the Land: pods, knights, owners, and guilds (the realm remodeled)]] — *this writ names the exported symbol `dealtGame`; this writ names the exported symbol `normalizeChronicle`*
- [[Writ — the Muster Library and the Intro Campaign]] — *this writ names the exported symbol `generateGrandMuster`*
- [[Writ — the task-language, the consequences, and the Regent's seat]] — *this writ names the exported symbol `dealtGame`*

---

*Generated by `tools/vault/emit.mjs` from `src/wargame-core.ts`:1. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run book`).*
