---
type: "module"
id: "module:src/table/FlatMapView.tsx"
title: "src/table/FlatMapView.tsx"
standing: "built"
standing_source: "knowledge/artifacts.json"
source_path: "src/table/FlatMapView.tsx"
source_line: 1
origin: "derived"
generator: "tools/vault/emit.mjs"
aliases:
  - "module:src/table/FlatMapView.tsx"
---

# src/table/FlatMapView.tsx

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree.  
> *Declared in `knowledge/artifacts.json`.*

836 lines · 2 exported symbols.

## What the file says of itself

> THE MAP — the realm drawn as an illustrated table, and wired to the records.
> 
> This replaces the live three.js scene (`src/realm/RealmView.tsx`). Edwin's
> call, 2026-07-28: *"I would love illustrated 2.5D we don't need 3d at all."*
> The browser composites flat art; it runs no 3D scene. What it draws was
> proven first as a baked artifact (`docs/frames/flat-proving-frame.html`) —
> that frame was a handsome picture whose buttons were demonstrations, because
> nothing in it could read the chronicle. This is the same drawing, computed in
> the app, over the doors the chronicle actually holds.
> 
> The firewall 

## Shape

- **Lines:** 836
- **Exported symbols (2):** `FlatMapView`, `FlatMapViewProps`
- **Assets it pulls in (no page, so no road):** `src/realm/realm.css`, `src/table/flatmap.css`

## Modules

- [[src/realm/Chrome.tsx]] — *imported by this file*
- [[src/realm/scene.ts]] — *imported by this file*
- [[src/table/fantasyRelief.ts]] — *imported by this file*
- [[src/table/flatMap.ts]] — *imported by this file*
- [[src/table/tableScene.ts]] — *imported by this file*

## Surfaces

- [[FlatMapView]] — *the same file `src/table/FlatMapView.tsx`, seen as a surface rather than a module*

## Backlinks

### Modules

- [[src/WarTableView.tsx]] — *imported by this file*

### Surfaces

- [[FlatMapView]] — *the same file `src/table/FlatMapView.tsx`, seen as a module rather than a surface; the same file, read as code*
- [[WarTableView]] — *imported by this view*

---

*Generated by `tools/vault/emit.mjs` from `src/table/FlatMapView.tsx`:1. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run book`).*
