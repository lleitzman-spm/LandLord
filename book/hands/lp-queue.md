---
type: "hand"
id: "hand:lp-queue"
title: "lp-queue"
standing: "built"
standing_source: "derived"
source_path: "src/domain/flows.ts"
source_line: 184
origin: "derived"
generator: "tools/vault/emit.mjs"
aliases:
  - "hand:lp-queue"
---

# lp-queue

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree.  
> *Derived from the tree — the code is there to be read.*

A QUEUE, not a person — a role nobody holds yet. It reads as the holder until a setting maps it to somebody.

## The source, verbatim

> holder: 'lp-queue'

*Verified against `src/domain/flows.ts`:184 on every lint — no quote, no object.*

> **This is a QUEUE, not a person.** A role nobody holds yet — it reads as the holder until a setting maps it to somebody. Every step below is work with no name against it.

## Modules

- [[src/domain/flows.ts]] — *declared in this module*

## Backlinks

### place

- [[Lease renewal: draft-offer]] — *its `holder` names `lp-queue` by id*
- [[Lease renewal: open-window]] — *its `holder` names `lp-queue` by id*
- [[Lease renewal: owner-window]] — *its `holder` names `lp-queue` by id*
- [[Lease renewal: post-fee]] — *its `holder` names `lp-queue` by id*
- [[Lease renewal: record]] — *its `holder` names `lp-queue` by id*
- [[Lease renewal: send-offer]] — *its `holder` names `lp-queue` by id*
- [[Lease renewal: tenant-response]] — *its `holder` names `lp-queue` by id*
- [[Move-out → re-list relay: deposit-transfer]] — *its `holder` names `lp-queue` by id*
- [[Move-out → re-list relay: owner-reserve]] — *its `holder` names `lp-queue` by id*
- [[Vendor dispatch: invoice-in]] — *its `holder` names `lp-queue` by id*
- [[Vendor dispatch: pay-vendor]] — *its `holder` names `lp-queue` by id*
- [[Vendor dispatch: post-to-accounting]] — *its `holder` names `lp-queue` by id*

---

*Generated by `tools/vault/emit.mjs` from `src/domain/flows.ts`:184. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run book`).*
