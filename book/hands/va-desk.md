---
type: "hand"
id: "hand:va-desk"
title: "va-desk"
standing: "built"
standing_source: "derived"
source_path: "src/domain/flows.ts"
source_line: 175
origin: "derived"
generator: "tools/vault/emit.mjs"
aliases:
  - "hand:va-desk"
---

# va-desk

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree.  
> *Derived from the tree — the code is there to be read.*

A QUEUE, not a person — a role nobody holds yet. It reads as the holder until a setting maps it to somebody.

## The source, verbatim

> holder: 'va-desk'

*Verified against `src/domain/flows.ts`:175 on every lint — no quote, no object.*

> **This is a QUEUE, not a person.** A role nobody holds yet — it reads as the holder until a setting maps it to somebody. Every step below is work with no name against it.

## Modules

- [[src/domain/flows.ts]] — *declared in this module*

## Backlinks

### place

- [[Move-out → re-list relay: turn-scope]] — *its `holder` names `va-desk` by id*
- [[Move-out → re-list relay: turn-work]] — *its `holder` names `va-desk` by id*
- [[Owner onboarding: make-ready]] — *its `holder` names `va-desk` by id*
- [[Owner onboarding: set-up-records]] — *its `holder` names `va-desk` by id*
- [[Owner onboarding: verify-insurance]] — *its `holder` names `va-desk` by id*
- [[Vendor dispatch: assign-vendor]] — *its `holder` names `va-desk` by id*
- [[Vendor dispatch: dispatch]] — *its `holder` names `va-desk` by id*
- [[Violation / notice: classify]] — *its `holder` names `va-desk` by id*
- [[Violation / notice: draft-notice]] — *its `holder` names `va-desk` by id*

---

*Generated by `tools/vault/emit.mjs` from `src/domain/flows.ts`:175. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run book`).*
