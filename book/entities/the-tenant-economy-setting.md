---
type: "entity"
id: "entity:tenant-setting"
title: "The tenant (economy) setting"
standing: "built"
standing_source: "knowledge/entities.json"
source_path: "src/domain/economySetting.ts"
source_line: 1
origin: "mined"
generator: "tools/vault/emit.mjs"
tags:
  - "economy"
  - "factory-setting"
aliases:
  - "entity:tenant-setting"
---

# The tenant (economy) setting

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree.  
> *Declared in `knowledge/entities.json`.*

The mechanism by which a deployment overrides the seed tenant's demo fee rates, spend caps and GL chart with its own, without any real figure living in the repository.

## The source, verbatim

> the mechanism by which a deployment overrides the seed
> // tenant's DEMO figures with its own, without any real figure ever living in
> // this repository

*Verified against `src/domain/economySetting.ts`:1 on every lint — no quote, no object.*

## Decisions that touched it

- [[Configuration, not constants]] — *declared in `knowledge/entities.json`*

## Modules

- [[src/domain/economySetting.ts]] — *the same file `src/domain/economySetting.ts`, seen as a module rather than a entity*

## Backlinks

### Decisions that touched it

- [[Configuration, not constants]] — *declared in `knowledge/decisions.json`*

### Modules

- [[src/domain/economySetting.ts]] — *the same file `src/domain/economySetting.ts`, seen as a entity rather than a module*

---

*Generated by `tools/vault/emit.mjs` from `src/domain/economySetting.ts`:1. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run book`).*
