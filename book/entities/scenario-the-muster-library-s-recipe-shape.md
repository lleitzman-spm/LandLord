---
type: "entity"
id: "entity:scenario"
title: "Scenario (the muster library's recipe shape)"
standing: "built"
standing_source: "knowledge/entities.json"
source_path: "src/domain/campaign.ts"
source_line: 1
origin: "mined"
generator: "tools/vault/emit.mjs"
tags:
  - "campaign"
  - "war-games"
aliases:
  - "entity:scenario"
---

# Scenario (the muster library's recipe shape)

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree.  
> *Declared in `knowledge/entities.json`.*

A recipe — a seed plus a declared shape — re-dealt against whatever the setting is; never a dumped event log, so a rename never butchers it.

## The source, verbatim

> A SCENARIO is a recipe, never a recording.

*Verified against `src/domain/campaign.ts`:1 on every lint — no quote, no object.*

## Facts it depends on

- [[Intro campaign — freshest backlog age at deploy]] — *the same file `src/domain/campaign.ts`, seen as a fact rather than a entity*
- [[Intro campaign act four — proposals to answer]] — *the same file `src/domain/campaign.ts`, seen as a fact rather than a entity*
- [[Intro campaign act six — a game month]] — *the same file `src/domain/campaign.ts`, seen as a fact rather than a entity*

## Decisions that touched it

- [[A muster is a recipe, never a recording]] — *declared in `knowledge/entities.json`*

## Modules

- [[src/domain/campaign.ts]] — *the same file `src/domain/campaign.ts`, seen as a module rather than a entity*

## Backlinks

### Facts it depends on

- [[Intro campaign — freshest backlog age at deploy]] — *declared in `knowledge/facts.json`; the same file `src/domain/campaign.ts`, seen as a entity rather than a fact*
- [[Intro campaign act four — proposals to answer]] — *declared in `knowledge/facts.json`; the same file `src/domain/campaign.ts`, seen as a entity rather than a fact*
- [[Intro campaign act six — a game month]] — *declared in `knowledge/facts.json`; the same file `src/domain/campaign.ts`, seen as a entity rather than a fact*

### Decisions that touched it

- [[The Intro Campaign is the default first deploy, reversible on Edwin's word]] — *declared in `knowledge/decisions.json`*

### Modules

- [[src/domain/campaign.ts]] — *the same file `src/domain/campaign.ts`, seen as a entity rather than a module*

### artifact

- [[docs/WRIT-THE-CAMPAIGN.md]] — *declared in `knowledge/artifacts.json`*

---

*Generated by `tools/vault/emit.mjs` from `src/domain/campaign.ts`:1. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run book`).*
