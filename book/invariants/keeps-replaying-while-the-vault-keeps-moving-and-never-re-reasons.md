---
type: "invariant"
id: "invariant:commit-append-003-keeps-replaying-while-the-vault-keeps-moving-and"
title: "keeps replaying while the vault keeps moving, and never re-reasons"
standing: "built"
standing_source: "derived"
source_path: "test/commit-append.test.ts"
source_line: 82
origin: "derived"
generator: "tools/vault/emit.mjs"
aliases:
  - "invariant:commit-append-003-keeps-replaying-while-the-vault-keeps-moving-and"
---

# keeps replaying while the vault keeps moving, and never re-reasons

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree.  
> *Derived from the tree — the code is there to be read.*

commitAppend — a minute of reasoning survives a concurrent write — keeps replaying while the vault keeps moving, and never re-reasons

## The source, verbatim

> keeps replaying while the vault keeps moving, and never re-reasons

*Verified against `test/commit-append.test.ts`:82 on every lint — no quote, no object.*

## Where it is enforced

- **Suite:** `test/commit-append.test.ts:82`
- **Under:** *commitAppend — a minute of reasoning survives a concurrent write*

## Modules

*These roads come from a shared source FILE, not from a semantic claim: the test file imports the module. Read it as "stands near", never as "proves".*

- [[src/server/vault.ts]] — *imported by the test FILE (shared source, not a claim about this one test)*

## Backlinks

*Nothing in the Book points here. An orphan page is worse than a missing one — it exists, it is correct, and no reader will ever reach it. `npm run book:lint` counts these.*

---

*Generated by `tools/vault/emit.mjs` from `test/commit-append.test.ts`:82. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run book`).*
