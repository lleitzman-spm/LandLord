---
type: "surface"
id: "surface:src/CommandPalette.tsx"
title: "CommandPalette"
standing: "built"
standing_source: "derived"
source_path: "src/CommandPalette.tsx"
source_line: 1
origin: "derived"
generator: "tools/vault/emit.mjs"
aliases:
  - "surface:src/CommandPalette.tsx"
---

# CommandPalette

> **STANDING — BUILT**  
> Implemented in code and checkable against the tree.  
> *Derived from the tree — the code is there to be read.*

React view in `src/CommandPalette.tsx` — 130 lines.

## Where it lives

- **File:** `src/CommandPalette.tsx`

> THE PALETTE — one door to every surface and every act.
> 
> This is what replaces a rail of nine glyphs and a footer of five buttons. It
> opens on ⌘K / Ctrl+K or `/`, filters as you type, and runs on Enter.
> 
> It also TEACHES: every command shows its own key beside it, so the player who
> reaches the Ledger by searching sees `g l` written next to it and reaches it
> that way the next time. A shortcut nobody is shown is a shortcut nobody uses.

## Modules

- [[src/CommandPalette.tsx]] — *the same file `src/CommandPalette.tsx`, seen as a module rather than a surface; the same file, read as code*
- [[src/keys.ts]] — *imported by this view*

## Backlinks

### Modules

- [[src/CommandPalette.tsx]] — *the same file `src/CommandPalette.tsx`, seen as a surface rather than a module*

---

*Generated by `tools/vault/emit.mjs` from `src/CommandPalette.tsx`:1. **Never edit this page** — it is a view, not an artifact. Fix the source and re-compile (`npm run book`).*
